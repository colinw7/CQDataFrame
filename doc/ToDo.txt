+ Named widget e.g. find src, pinned to area
  + enable custom edit ? just command args
+ store all details in cell and allow rerun
+ vi/terminal cell (CQTty)
+ file editor (CQEdit)
+ save/restore (history)
+ complete for more commands. Improve display command
+ reload for all widgets which depend on external state
+ handle escape codes in output (format text to parts ?)
+ more canvas commands
+ xml format for table data (CUsage)
+ title string
+ add data frame widget to be used standalone
